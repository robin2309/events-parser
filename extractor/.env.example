# Application
NODE_ENV=development
LOG_LEVEL=info

# Rate limiting (requests per second per adapter)
RATE_LIMIT_RPS=2

# HTTP timeouts (milliseconds)
HTTP_TIMEOUT=30000
HTTP_RETRY_ATTEMPTS=3

# Database
DATABASE_URL=postgresql://events:events@localhost:5432/events
